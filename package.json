{
    "name": "bun-elysia",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "format": "prettier . --write",
        "dev": "bun run --watch index.ts",
        "prod": "bun run index.ts"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "prettier": "^3.6.2"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
